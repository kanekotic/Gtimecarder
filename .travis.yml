sudo: true

language: node_js
node_js:
  - "8"

before_install:
  - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
  - sudo dpkg -i google-chrome*.deb

script:
  - yarn test:cov
  - /usr/bin/google-chrome --pack-extension=./dist